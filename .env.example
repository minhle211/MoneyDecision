# Copy to .env and fill in. Do not commit .env.

# Database (used when running backend outside Docker)
DATABASE_URL=postgresql://finbud:finbud@localhost:5432/finbud

# JWT – use a long random string in production
SECRET_KEY=change-me-in-production-use-env

# Optional: OpenAI for "Mentor Note" feature
# OPENAI_API_KEY=sk-...
